package interfaces;

public interface Pagavel {
    
    // não precisa dizer se é público ou privado
    // todos os métodos em uma interface são considerados públicos e abstratos
    
    double gerarPagamento();
}
